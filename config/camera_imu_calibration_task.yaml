schema_version: 1
job: camera_imu_calibration

dataset:
  type: bag
  path: data/imu_camera_bag

target:
  path: config/aprilgrid.yaml

camera_calibration:
  path: config/camchain.yaml

imus:
  - {path: config/imu.yaml, model: calibrated}
